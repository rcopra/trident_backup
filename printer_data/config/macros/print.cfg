####################################################################################
####################################################################################


[gcode_macro Z_TILT_ADJUST]     # IDM/Cartographer optimized 3Z leveling macro
rename_existing: _Z_TILT_ADJUST
gcode:
    SAVE_GCODE_STATE NAME=STATE_Z_TILT         # Save current state for Z tilt adjustment
    # Change LED status
    STATUS_LEVELING
    BED_MESH_CLEAR                             # Clear bed mesh
    {% if not printer.z_tilt.applied %}
      _Z_TILT_ADJUST horizontal_move_z=10 retry_tolerance=1   # Adjust Z tilt with higher tolerance initially
    {% endif %}
    _Z_TILT_ADJUST horizontal_move_z=2          # Fine-tune Z tilt adjustment
    RESTORE_GCODE_STATE NAME=STATE_Z_TILT       # Restore saved state after adjustment

# [gcode_macro FORM_TIP]
# gcode:
#       SAVE_GCODE_STATE NAME=FORMTIPSTATE
#       G91
#       G0 E15 F360
#       G0 E5 F3600
#       G0 E-22 F3600
#       G0 E2 F300
#       G0 E-2.1 F300
#       G0 E2 F300
#       G0 E-2.2 F300
#       G0 E2 F300
#       G0 E-2.3 F300
#       G0 E2 F300
#       G0 E-2.4 F300
#       G0 E2 F300
#       G0 E-2.5 F300
#       G0 E2 F300
#       G0 E-43 F300
#       G90
#       RESTORE_GCODE_STATE NAME=FORMTIPSTATE


# [gcode_macro UNLOAD_FILAMENT]
# gcode:
#     SAVE_GCODE_STATE NAME=<beforeunload>
#     M83 # Set extruder to absolute mode
#     G1 E10 F{5*60}    # Extrude a little to soften tip
#     G0 E-35 F{60*60}   # Extract filament to cold end
#     G0 E-100 F{5*60}   # Continue extraction slow allow filament to be cooled enough before reaching the gears
#     M400
#     RESTORE_GCODE_STATE NAME=<beforeunload>

# [gcode_macro LOAD_FILAMENT] #load filament
# gcode:
#     SAVE_GCODE_STATE NAME=<beforeload>
#     M83
#     G1 E100 F{10*60}
#     G1 E40 F{5*60}
#     M400
#     RESTORE_GCODE_STATE NAME=<beforeload>

# [gcode_macro CLEAN_NOZZLE]
# gcode:
#   SAVE_GCODE_STATE NAME=CLEAN_NOZZLE_STATE
#   G90
#   G0 Z15 F5000
#   G0 X235 Y355 F6000
#   {% for wipe in range(8) %}
#     {% for coordinate in [(235,356.0),(275.0,356.0)] %}
#       G0 X{coordinate[0]} Y{coordinate[1] - 0.25 * wipe} Z-0.2 F6000
#     {% endfor %}
#   {% endfor %}
#   G0 Z15 F5000
#   G0 X170 Y170 F5000
#   RESTORE_GCODE_STATE NAME=CLEAN_NOZZLE_STATE

[gcode_macro _RESET_VELOCITY_LIMIT]
description: Restore all velocity limits to configured defaults
gcode:
    SET_VELOCITY_LIMIT VELOCITY={printer.configfile.settings.printer.max_velocity}
    SET_VELOCITY_LIMIT ACCEL={printer.configfile.settings.printer.max_accel}
    SET_VELOCITY_LIMIT MINIMUM_CRUISE_RATIO={printer.configfile.settings.printer.minimum_cruise_ratio}
    SET_VELOCITY_LIMIT SQUARE_CORNER_VELOCITY={printer.configfile.settings.printer.square_corner_velocity}
    M220 S100
    M221 S100

####################################################################################
##                               Probe Calibration Macro
####################################################################################
[gcode_macro PROBECALIBRATE]
# Defines a G-code macro for probe calibration
gcode:
    G28                          # Home all axes
    G0 X175 Y175 Z1 F3600        # Fast move to X150 Y150 Z1 at 3600 mm/min
    PROBE_CALIBRATE              # Start probe calibration

####################################################################################
##                               Bed Leveling and Height Calibration Macro
####################################################################################
[gcode_macro G32]
# Defines a G-code macro for bed leveling and height calibration
gcode:
    BED_MESH_CLEAR               # Clear bed mesh
    G28                          # Home all axes
    Z_TILT_ADJUST                # Perform gantry leveling
    G28                          # Home all axes
    G0 X175 Y175 Z30 F3600       # Fast move to X150 Y150 Z30 at 3600 mm/min

####################################################################################
##                              Print Start (Bedfans Version) 
####################################################################################

[gcode_macro PRINT_START]
gcode:
  # This part fetches data from your slicer. Such as bed, extruder, and chamber temps and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set target_chamber = params.CHAMBER|default("45")|int %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}
  {% set initial_tool = params.TOOL|default("0")|int %}
  {% set material = params.MATERIAL | default("UNKNOWN") %}

  # Clear any lingering pause states
  CLEAR_PAUSE

  # Home the printer, set absolute positioning and update the Stealthburner LEDs.
  STATUS_HOMING                                         # Set LEDs to homing-mode
  G28                                                   # Full home (XYZ)
  G90                                                   # Absolute position
  # AFC_PARK

  #  BED MESH (1/2)
  BED_MESH_CLEAR                                       # Clear old saved bed mesh (if any)

  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak.
  {% if params.BED|int > 90 %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    STATUS_HEATING                                      # Set LEDs to heating-mode
    M106 S255                                           # Turn on the PT-fan

    ##  Uncomment if you have a Nevermore.
    # SET_PIN PIN=nevermore VALUE=1                      # Turn on the nevermore

    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{target_bed}                                  # Set the target temp for the bed
    SET_DISPLAY_TEXT MSG="Heatsoak: {target_chamber}c"  # Display info on display
    TEMPERATURE_WAIT SENSOR="temperature_sensor chamber" MINIMUM={target_chamber}   # Waits for chamber temp

  # If the bed temp is not over 90c, then skip the heatsoak and just heat up to set temp
  {% else %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    STATUS_HEATING                                      # Set LEDs to heating-mode
    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{target_bed}                                  # Set the target temp for the bed
  {% endif %}

  # Heat hotend to 150c. This helps with getting a correct Z-home.
  SET_DISPLAY_TEXT MSG="Hotend: 145c"                   # Display info on display
  M109 S145                                             # Heat hotend to 150c

  #  Z_TILT_ADJUST
  SET_DISPLAY_TEXT MSG="Leveling"                      # Display info on display
  STATUS_LEVELING                                      # Set LEDs to leveling-mode
  Z_TILT_ADJUST                                        # Level the printer via Z_TILT_ADJUST
  G28 Z                                                # Home Z again after Z_TILT_ADJUST

  # Wipe nozzle for Cartographer Touch
  AFC_BRUSH
  
  # Cartographer Touch
  SET_DISPLAY_TEXT MSG="Cartographer Touch"
  CARTOGRAPHER_TOUCH_HOME

  # Apply material-specific Z offset
  _SET_CARTO_OFFSET MATERIAL={material}

  # BED MESH (2/2)
  SET_DISPLAY_TEXT MSG="Bed mesh"                       # Display info on display
  STATUS_MESHING                                        # Set LEDs to bed mesh-mode
  BED_MESH_CALIBRATE ADAPTIVE=1 PROFILE=print_mesh      # Start the bed mesh (add ADAPTIVE=1) for adaptive bed mesh

  AFC_PARK
  T{initial_tool}                                       # Load Initial Tool

  # Heat up the hotend up to target via data from slicer
  SET_DISPLAY_TEXT MSG="Hotend: {target_extruder}c"     # Display info on display
  STATUS_HEATING                                        # Set LEDs to heating-mode
  # G1 X{x_wait} Y{y_wait} Z15 F9000                      # Go to center of the bed
  M107                                                  # Turn off partcooling fan
  M109 S{target_extruder}                               # Heat the hotend to set temp


  # Get ready to print by doing a primeline and updating the LEDs
  SET_DISPLAY_TEXT MSG="Printer goes brr"               # Display info on display
  STATUS_PRINTING                                       # Set LEDs to printing-mode
  G0 Z0.4                                               # Raise Z to 0.4
  G91                                                   # Incremental positioning
  SQUIGGLY_PURGE
  G90                                                   # Absolute position

  # Fix that skew 
 SKEW_PROFILE LOAD=Califlower

####################################################################################
##                              Print Start (Fake Heater Version) 
####################################################################################

# [gcode_macro PRINT_START]
# gcode:
#   # Fetch slicer-provided parameters
#   {% set target_bed = params.BED|int %}
#   {% set target_extruder = params.EXTRUDER|int %}
#   {% set target_chamber = params.CHAMBER|default("40")|int %}
#   {% set start_chambertemp = target_chamber - 10 %}
#   {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
#   {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}
#   {% set initial_tool = params.TOOL|default("0")|int %}
#   {% set material = params.MATERIAL | default("UNKNOWN") %}
# 
#   # Determine temporary bed boost based on chamber target
#   {% set bed_boost = 0 %}
#   {% if target_chamber >= 40 and target_chamber < 55 %}
#     {% set bed_boost = 25 %}
#   {% elif target_chamber >= 55 %}
#     {% set bed_boost = 18 %}
#   {% endif %}
# 
#   {% set requested_soak_bed = target_bed + bed_boost %}
#   {% set soak_bed_target = [requested_soak_bed, 120] | min %}
# 
#   # Clear any lingering paused state
#   CLEAR_PAUSE
# 
#   # Home all axes and switch to absolute positioning
#   STATUS_HOMING
#   G28
#   G90
# 
#   # Clear any previously loaded bed mesh
#   BED_MESH_CLEAR
# 
#   # Bed heatup logic with 10 percent tolerance
#   {% set current_bed_temp = printer.heater_bed.temperature %}
#   {% set tolerance_percentage = 0.10 %}
#   {% set lower_bound = soak_bed_target * (1 - tolerance_percentage) %}
# 
#   STATUS_HEATING
# 
#   {% if current_bed_temp < lower_bound %}
#     {% if bed_boost > 0 %}
#       RESPOND MSG="Heat soak active. Bed target: {soak_bed_target}C"
#     {% endif %}
#     M190 S{soak_bed_target}      # Heat bed and wait
#   {% else %}
#     M140 S{soak_bed_target}      # Set bed target without waiting
#     RESPOND MSG="Bed already within 10% of {soak_bed_target}C"
#   {% endif %}
# 
#   # Start chamber heating and raise toolhead for heat soak
#   {% if params.CHAMBER|default(0)|int > 0 %}
#     SET_CHAMBER CHAMBER_TEMP={target_chamber}
#     G0 Z80                      # Move bed lower during chamber warmup
#   {% endif %}
# 
#   # Wait until chamber reaches initial stabilization temperature
#   RESPOND MSG="Waiting for chamber to reach {start_chambertemp}C"
#   {% if start_chambertemp > 10 %}
#     TEMPERATURE_WAIT SENSOR="heater_generic heater_chamber" MINIMUM={start_chambertemp}
#   {% else %}
#     TEMPERATURE_WAIT SENSOR="heater_generic heater_chamber" MINIMUM=0
#   {% endif %}
# 
#   # Restore bed to print temperature after chamber warmup
#   {% if bed_boost > 0 %}
#     RESPOND MSG="Chamber warm. Restoring bed to {target_bed}C"
#     M140 S{target_bed}
#   {% endif %}
# 
#   # Raise bed in preparation for leveling
#   G0 Z10
# 
#   # Preheat hotend for Cartographer Touch 
#   RESPOND MSG="Hotend: 145C"
#   M109 S145
# 
#   # Z Tilt
#   RESPOND MSG="Leveling"
#   STATUS_LEVELING
#   Z_TILT_ADJUST
#   G28 Z
# 
#   # Nozzle cleaning and Cartographer Touch 
#   AFC_BRUSH
#   RESPOND MSG="Cartographer Touch"
#   CARTOGRAPHER_TOUCH_HOME
# 
#   # Apply material-specific Z offset
#   _SET_CARTO_OFFSET MATERIAL={material}
# 
#   # Create Bed Mesh 
#   RESPOND MSG="Bed mesh"
#   STATUS_MESHING
#   BED_MESH_CALIBRATE ADAPTIVE=1 PROFILE=print_mesh
# 
#   # Park toolhead and load initial tool
#   AFC_PARK
#   T{initial_tool}
# 
#   # Heat hotend to final print temperature
#   RESPOND MSG="Hotend: {target_extruder}C"
#   STATUS_HEATING
#   M107
#   M109 S{target_extruder}
# 
#   # Final preparation and (squiggle)purge
#   RESPOND MSG="Printer goes brr"
#   STATUS_PRINTING
#   G0 Z0.4
#   G91
#   SQUIGGLY_PURGE
#   G90
# 
#   # Fix that skew 
#  SKEW_PROFILE LOAD=Califlower

#--------------------------------------------------------------------
[gcode_macro PRINT_END]
gcode:
    {% set th = printer.toolhead %}
    {% set x_safe = th.position.x + 20 * (1 if th.axis_maximum.x - th.position.x > 20 else -1) %}
    {% set y_safe = th.axis_maximum.y - 5 %}
    {% set z_safe = th.position.z + (50 if th.position.z + 50 <= th.axis_maximum.z else th.axis_maximum.z - th.position.z) %}

    SAVE_GCODE_STATE NAME=STATE_PRINT_END

    M400                           ; wait for buffer to clear
    G92 E0                         ; zero the extruder
    G1 E-2.0 F3600                 ; retract filament

    TURN_OFF_HEATERS

    G90                                      ; absolute positioning
    G0 X{x_safe} Y{y_safe} Z{z_safe} F20000  ; move nozzle to remove stringing
    G0 X{th.axis_maximum.x//2} Y{y_safe} F18000  ; park nozzle at rear
    M107                                     ; turn off fan

    BED_MESH_CLEAR

    RESTORE_GCODE_STATE NAME=STATE_PRINT_END MOVE=0

    _RESET_VELOCITY_LIMIT
    SET_SKEW CLEAR=1


#--------------------------------------------------------------------
[gcode_macro PAUSE]
description: Pause the actual running print
rename_existing: PAUSE_BASE
# change this if you need more or less extrusion
variable_extrude: 1.0
gcode:
  ##### read E from pause macro #####
  {% set E = printer["gcode_macro PAUSE"].extrude|float %}
  ##### set park positon for x and y #####
  # default is your max posion from your printer.cfg
  # {% set x_park = printer.toolhead.axis_maximum.x|float - 5.0 %}
  # {% set y_park = printer.toolhead.axis_maximum.y|float - 5.0 %}
  {% set x_park = 300 %}
  {% set y_park = 0 %}
  ##### calculate save lift position #####
  {% set max_z = printer.toolhead.axis_maximum.z|float %}
  {% set act_z = printer.toolhead.position.z|float %}
  {% if act_z < (max_z - 2.0) %}
      {% set z_safe = 50.0 %}
  {% else %}
      {% set z_safe = max_z - act_z %}
  {% endif %}
  ##### end of definitions #####
  PAUSE_BASE
  G91
  {% if printer.extruder.can_extrude|lower == 'true' %}
    G1 E-{E} F2100
  {% else %}
    {action_respond_info("Extruder not hot enough")}
  {% endif %}
  {% if "xyz" in printer.toolhead.homed_axes %}
    G1 Z{z_safe} F900
    G90
    G1 X{x_park} Y{y_park} F6000
  {% else %}
    {action_respond_info("Printer not homed")}
  {% endif %}
#--------------------------------------------------------------------
[gcode_macro RESUME]
description: Resume the actual running print
rename_existing: RESUME_BASE
gcode:
  ##### read E from pause macro #####
  {% set E = printer["gcode_macro PAUSE"].extrude|float %}
  #### get VELOCITY parameter if specified ####
  {% if 'VELOCITY' in params|upper %}
    {% set get_params = ('VELOCITY=' + params.VELOCITY)  %}
  {%else %}
    {% set get_params = "" %}
  {% endif %}
  ##### end of definitions #####
  {% if printer.extruder.can_extrude|lower == 'true' %}
    G91
    G1 E{E} F2100
  {% else %}
    {action_respond_info("Extruder not hot enough")}
  {% endif %}
  RESUME_BASE {get_params}
#--------------------------------------------------------------------

[gcode_macro CANCEL_PRINT]
# Defines a G-code macro to cancel the actual running print
description = Cancel the actual running print
rename_existing = CANCEL_PRINT_BASE
variable_park = True
gcode =
    G91
    G0 Z10
    G90
    G28 Y                                     # Home Y axis
    _TOOLHEAD_PARK_PAUSE_CANCEL               # Call _TOOLHEAD_PARK_PAUSE_CANCEL macro
    TURN_OFF_HEATERS                          # Turn off all heaters
    CANCEL_PRINT_BASE                         # Call CANCEL_PRINT_BASE to cancel print

   _RESET_VELOCITY_LIMIT

[gcode_macro SET_CHAMBER]
gcode:
  {% set chamber_target = params.CHAMBER_TEMP|default(0)|float %}
  SET_HEATER_TEMPERATURE heater=heater_chamber target={chamber_target}


[gcode_macro DATA_SAMPLE]
gcode:
  G90
  M106 S255
  RESPOND TYPE=command MSG='Waiting for Coil to cool to 40'
  M117 Waiting for Coil to cool to 40
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MAXIMUM=40
  RESPOND TYPE=command MSG='Starting Phase 1 of 4'
  M117 Starting Phase 1 of 4
  M106 S0
  G28
  G0 Z1
  M104 S250
  M140 S110
  G4 P1000
  CARTOGRAPHER_STREAM FILENAME=data1
  M117 Waiting for Coil to heat to 70
  RESPOND TYPE=command MSG='Waiting for Coil to heat to 70'
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MINIMUM=55
  CARTOGRAPHER_STREAM FILENAME=data1
  M104 S0
  M140 S0
  M106 S255
  G0 Z80
  RESPOND TYPE=command MSG='Waiting for Coil to cool to 40'
  M117 Waiting for Coil to cool to 40
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MAXIMUM=40

  M117 Starting Phase 2 of 4
  RESPOND TYPE=command MSG='Starting Phase 2 of 4'
  M106 S0
  G28 Z0
  G0 Z2
  M104 S250
  M140 S110
  G4 P1000
  CARTOGRAPHER_STREAM FILENAME=data2
  M117 Waiting for Coil to heat to 70
  RESPOND TYPE=command MSG='Waiting for Coil to heat to 70'
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MINIMUM=55
  CARTOGRAPHER_STREAM FILENAME=data2
  M104 S0
  M140 S0
  M106 S255
  G0 Z80
  RESPOND TYPE=command MSG='Waiting for Coil to cool to 40'
  M117 Waiting for Coil to cool to 40
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MAXIMUM=40

  M117 "Starting Phase 3 of 4"
  RESPOND TYPE=command MSG='Starting Phase 3 of 4'
  M106 S0
  G28 Z0
  G0 Z3
  M104 S250
  M140 S110
  G4 P1000
  CARTOGRAPHER_STREAM FILENAME=data3
  M117 Waiting for Coil to heat to 70
  RESPOND TYPE=command MSG='Waiting for Coil to heat to 70'
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MINIMUM=55
  CARTOGRAPHER_STREAM FILENAME=data3
  M104 S0
  M140 S0
  M106 S255
  G0 Z80
  M117 Waiting for Coil to cool to 40
  RESPOND TYPE=command MSG='Waiting for Coil to cool to 40'
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MAXIMUM=40

  M117 "Starting Phase 4 of 4"
  RESPOND TYPE=command MSG='Starting Phase 4 of 4'
  M106 S0
  G28 Z0
  G0 Z5
  M104 S250
  M140 S110
  G4 P1000
  CARTOGRAPHER_STREAM FILENAME=data4
  M117 Waiting for Coil to heat to 70
  RESPOND TYPE=command MSG='Waiting for Coil to heat to 70'
  TEMPERATURE_WAIT SENSOR='temperature_sensor cartographer_coil' MINIMUM=55
  CARTOGRAPHER_STREAM FILENAME=data4
  M104 S0
  M140 S0
  RESPOND TYPE=command MSG='Testing complete, please move files using: mv ~/klipper/data1 ~/klipper/data2 ~/klipper/data3 ~/klipper/data4 ~/cartographer-klipper/'
  M117 "Testing complete, please move files using: mv ~/klipper/data1 ~/klipper/data2 ~/klipper/data3 ~/klipper/data4 ~/cartographer-klipper/"
  RESPOND TYPE=command MSG='Follow the remaining instructions here: https://docs.cartographer3d.com/cartographer-probe/advanced-features/temperature-differential-calibration-beta'
  M117 "Follow the remaining instructions here: https://docs.cartographer3d.com/cartographer-probe/advanced-features/temperature-differential-calibration-beta"
